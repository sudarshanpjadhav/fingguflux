# FingguFlux Svelte Adapter

Zero-runtime, high-performance headless UI components for Svelte.

> [!WARNING]
> **Svelte 5 Required:** This adapter relies on the Svelte 5 AST structure for deterministic class mapping. It requires `svelte@>=5.0.0` and is not compatible with Svelte 3 or 4.
